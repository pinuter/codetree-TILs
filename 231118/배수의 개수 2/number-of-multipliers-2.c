#include <stdio.h>

int main() {
    int n, cnt = 0;
    
    for(int i = n; i <= n; i++){
        scanf("%d", &n);
        if(i % 2 == 1){
            cnt++;
        
        }
        
    }
    printf("%d", cnt);
    return 0;
}